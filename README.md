# GoImageMagick

Builds small badges with a grade and a title.

## Install

#### Build docker container with docker compose
```sh
cd Docker && docker-compose build 
```

#### Get container name via

```sh
docker ps
```

#### Install go dependencies with

```sh
docker exec -t __YOURCONTAINERNAME__ go get
```
